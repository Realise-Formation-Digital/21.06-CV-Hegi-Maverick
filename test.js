
function findFirstTreeMinNumber(list) {

    var listComplet = [4, 10, 5, 1, 3, 2]
    var listmin = 0;

    for (let p = 0; 6; p++) {

        if (p === 1) { min = list[0] }

        else {
            if (p < listmin) {
                listmin = list[p]

                if (listmin = 3) { print }
            }
        }
    }

}
var list = [4, 10, , 5, 1, 3, 2]
findFirstTreeMinNumber(list);
////////////////////////////////////////////////////////


/**
 * 
 */

//

function findPairNumber() {

    var listComplet = [2, 5, 3, 7, 8]
    var listpair = 0;
    var result = 0

    for (let p = 0; 5; p++) {

        var resultat = listComplet[p] / 2

    }

}









/////////////////////////////////////////////////

function check() {
    var nbr;
    nbr = Number(document.getElementById("myInput").value);
    if (nbr % 2 == 0) {
        alert("Nombre pair");
    }
    else {
        alert("Nombre impair");
    }
}


////////////////////////////////////////////////////////
//if (Number.isInteger(4)===true){fjksdj elese fdfkjl}


function fits() {
    if (Number.isInteger(p) === false) {
        return 'Fits!';
    }
    return 'Does NOT fit!';
}

console.log(fits(5, 10));
// expected output: "Fits!"

console.log(fits(5, 11));
// expected output: "Does NOT fit!"


findPairNumber();





function findMinNumber() {


    var listComplet = [4, 10, 5, 1, 3, 2]
    var listmin = 0;

    for (let p = 0; 6; p++) {

        if (p === 1) { min = list[0] }
        else {
            if (pos < listmin) {
                listmin = list[p]
            }
        }

    }

}


//////////////////////////////////////////////

// var listcomplet = [22, 11, 33, 12, 5, 10];
// var listpair = [0, 0, 0];
// var cont = 0;

// for (let p = 0; p < 6; p++) {

//     if (Number.isInteger(listcomplet[p] / 2) === true) {

//         listpair[cont] = listcomplet[p];
//         cont++;
//     }
// } 
// console.log(listpair);
